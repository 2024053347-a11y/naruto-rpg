# V2.1 服务器配置更新指�?
亲爱的服�?开发者：

我们在本次更新中大幅增强�?*云存档系�?*，引入了自动同步机制、最�?200mB 的存档空间，并深度优化了【个人中心】的 UI 界面�?
同时，针对有玩家反馈�?`413 Request Entity Too Large` 报错，我们也查明了原因：**这不是游戏代码的问题，而是服务�?Nginx 的默认限制造成的�?* 

要让本次更新完全生效，且不再出现 413 报错，您必须在您的服务器上进行以下操作：

## 1. 解决 Nginx 413 报错 (关键�?

Nginx 默认限制客户端上传的内容最大为 `1MB`。当游戏进行�?20 回合左右，积累的记忆和上下文会导致请求体积变大，Nginx 就会直接掐断请求，报�?413。此外，我们新的云存档允许最�?200mB 的体积，同样会被 Nginx 拦截�?
**如果您使用的是宝塔面�?(BT Panel)�?*
1. 登录宝塔面板，进入【网站】�?2. 找到您部署此游戏的网站，点击右侧的【设置】�?3. 在弹出的窗口左侧选择【配置文件】�?4. 找到 `server { ... }` 块，在里面加入（或修改）这一行：
   ```nginx
   client_max_body_size 200m;
   ```
5. 保存配置文件，并在宝塔的【软件商店】中找到 Nginx，点击【重启】�?
**如果您使用的是普�?Linux 环境部署�?Nginx�?*
1. 打开 Nginx 的全局配置文件（通常位于 `/etc/nginx/nginx.conf`）或者当前站点的配置文件（位�?`/etc/nginx/conf.d/` �?`sites-available/`）�?2. �?`http { ... }` �?`server { ... }` 块中，添加或修改�?   ```nginx
   client_max_body_size 200m;
   ```
3. 测试并重�?Nginx�?   ```bash
   sudo nginx -t
   sudo systemctl restart nginx
   ```

## 2. 重启 Node.js 后端

本次更新中，我们已经修改�?`server/index.js` �?`server/config.js`�?- Express 后端 JSON 接收限制提升到了 `200mb`�?- 云存档限制改为了 `每个用户 1 个存档`，最�?`200mB`�?
请在您的服务器终端中，重�?Node.js 进程，以使新代码生效�?如果您使用的�?`pm2` 管理进程，请执行�?```bash
pm2 restart all
```
（或者使用您具体�?pm2 进程名，�?`pm2 restart naruto-rpg`）�?
---

完成以上两步后，玩家即可体验到全新的自动云同步存档，并且即使游玩几千个回合，也不会再遇到 `413 Request Entity Too Large` 的错误了�?
